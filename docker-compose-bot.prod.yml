version: "3.9"

services:
  fitness_bot:
    image: ivankhoda/fitness:fitness_bot-${FITNESS_BOT_TAG}
    ports:
      - "5000:5000"
    env_file:
      - .env/production/fitness_bot
    restart: unless-stopped
    networks:
      - fit

networks:
  fit:
    driver: bridge
    attachable: true
